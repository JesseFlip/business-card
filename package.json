{
  "name": "business-card-fullstack",
  "version": "1.0.0",
  "description": "Full stack portfolio site for Jesse Flippen",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "lint:html": "html-validate public/index.html",
    "lint:js": "node --check server.js && node --check public/script.js",
    "lint": "npm run lint:html && npm run lint:js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "html-validate": "^8.18.1"
  }
}
